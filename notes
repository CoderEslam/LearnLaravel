when you make now route file .php
RouteServiceProvider and link your new route file

Route::middleware('web')
                ->namespace($this->namespace)
                ->group(base_path('routes/adminWeb.php'));


/* Folder == namespace  => group of route*/


in Route::group() =? best case fro make auth and prefix

to make controller we write -> php artisan make:controller {Name of Controller}

to make controller with all function we write -> php artisan make:controller PenController --resource


